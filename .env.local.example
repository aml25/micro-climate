# Server-side only (Synoptic Data API token — do NOT prefix with NEXT_PUBLIC_)
SYNOPTIC_API_TOKEN=your_token_here

# Public (Mapbox token — safe to expose to browser)
NEXT_PUBLIC_MAPBOX_TOKEN=your_token_here
